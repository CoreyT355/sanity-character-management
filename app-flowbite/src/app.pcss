/* Write your global styles here, in PostCSS syntax */
@tailwind base;
@tailwind components;
@tailwind utilities;

.corner-cut {
  clip-path: polygon(
    0% 16px,
    16px 0%,
    100% 0%,
    100% 0%,
    100% calc(100% - 16px),
    calc(100% - 16px) 100%,
    0% 100%,
    0% 100%
  );
}
